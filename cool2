local Photon = {}
Photon.__index = Photon

function Photon:CreateWindow(options)
    -- Store options for later use (UI, config, Discord, KeySystem, etc.)
    local self = setmetatable({}, {__index = {}})
    self.Options = options
    -- TODO: Build the actual UI here
    return self
end

-- Add stubs for all Rayfield-style methods
function Photon.CreateTab(self, name, icon)
    -- TODO: Create a tab in the sidebar
    return setmetatable({}, {__index = {}})
end

function Photon.CreateSection(self, name)
    -- TODO: Create a section in the current tab
    return setmetatable({}, {__index = {}})
end

function Photon.CreateToggle(self, options)
    -- TODO: Create a toggle control
    return setmetatable({}, {__index = {}})
end

function Photon.CreateSlider(self, options)
    -- TODO: Create a slider control
    return setmetatable({}, {__index = {}})
end

function Photon.CreateDropdown(self, options)
    -- TODO: Create a dropdown control
    return setmetatable({}, {__index = {}})
end

function Photon.CreateKeybind(self, options)
    -- TODO: Create a keybind control
    return setmetatable({}, {__index = {}})
end

function Photon.CreateColorPicker(self, options)
    -- TODO: Create a color picker control
    return setmetatable({}, {__index = {}})
end

-- ...add more as needed (Button, Label, Paragraph, Input, Notification, etc.)

return setmetatable({}, Photon)
